box: wercker/golang
build:
    steps:
        - setup-go-workspace
        - script:
            code: go get github.com/tools/godep
        - script:
            name: go dependencies
            code: godep go build ./... && godep go install ./...
        - script:
            name: go tests
            code: godep go test ./...
        - script:
            code: echo "okay, done."
deploy:
    steps:
        - joshuamckenty/cloud-foundry-deploy
